@model IEnumerable<GameHaven.Models.Models.Game>

@{
    ViewData["Title"] = "Computer Games"; <!-- Set the page title -->
    ViewData["HideHeaderSearch"] = true; <!-- Hide the main header search, only use page-specific search -->
}

<div class="container mt-4">
    <!-- Search & Sort Section -->
    <form method="get" asp-controller="Computer" asp-action="Games" class="row g-2 mb-4">

        <!-- Search Input Column -->
        <div class="col-md-8">
            <input type="text"
                   name="q"
                   value="@(ViewData["CurrentSearch"] ?? "")"
                   class="form-control"
                   placeholder="Search" />
        </div>

        <!-- Sort Dropdown Column -->
        <div class="col-md-4">
            <select name="sort" class="form-select" onchange="this.form.submit()"> <!-- Submit form on change -->
                <option value="">Sort By</option> <!-- Default option -->

                <!-- Sort by Price Ascending -->
                @if (ViewData["CurrentSort"]?.ToString() == "price-asc")
                {
                    <option value="price-asc" selected>Price: Low → High</option>
                }
                else
                {
                    <option value="price-asc">Price: Low → High</option>
                }

                <!-- Sort by Price Descending -->
                @if (ViewData["CurrentSort"]?.ToString() == "price-desc")
                {
                    <option value="price-desc" selected>Price: High → Low</option>
                }
                else
                {
                    <option value="price-desc">Price: High → Low</option>
                }

                <!-- Sort by Name Ascending -->
                @if (ViewData["CurrentSort"]?.ToString() == "name-asc")
                {
                    <option value="name-asc" selected>Name: A → Z</option>
                }
                else
                {
                    <option value="name-asc">Name: A → Z</option>
                }

                <!-- Sort by Name Descending -->
                @if (ViewData["CurrentSort"]?.ToString() == "name-desc")
                {
                    <option value="name-desc" selected>Name: Z → A</option>
                }
                else
                {
                    <option value="name-desc">Name: Z → A</option>
                }
            </select>
        </div>
    </form>
</div>

<div class="container mt-4">
    <!-- Computer Games Promotion Banner -->
    <div class="mb-5">
        <img src="~/images/computer/promotions/computer-games-promo-2.png"
             class="promo-banner"
             alt="Computer Games Promotion"> <!-- Banner image -->
    </div>

    <!-- Section Heading -->
    <h2 class="mb-3 fw-bold">Computer Games</h2>

    <!-- Product Cards Grid -->
    <div class="row g-4">
        @foreach (var item in Model)
        {
            <div class="col-md-3">
                <div class="card h-100 shadow-sm border-0 hover-shadow">
                    <!-- Link to product details -->
                    <a asp-controller="Computer"
                       asp-action="Details"
                       asp-route-slug="@item.Slug"
                       asp-route-category="@item.Category"
                       class="text-decoration-none text-dark">
                        <!-- Product image -->
                        <img src="@Url.Content(item.ImageUrl)"
                             class="card-img-top"
                             alt="@item.Title"
                             style="object-fit: contain; max-height: 400px;">
                        <!-- Product title and price -->
                        <div class="card-body text-center">
                            <h5 class="card-title fw-bold">@item.Title</h5>
                            <p class="text-muted mb-2">£@item.Price</p>
                        </div>
                    </a>

                    <!-- Add to Basket button -->
                    <div class="text-center pb-3">
                        <form asp-controller="Home" asp-action="AddToBasket" method="post">
                            <input type="hidden" name="id" value="@item.Slug" /> <!-- Hidden product ID -->
                            <input type="hidden" name="title" value="@item.Title" /> <!-- Hidden product title -->
                            <input type="hidden" name="price" value="@item.Price" /> <!-- Hidden product price -->
                            <input type="hidden" name="image" value="@item.ImageUrl" /> <!-- Hidden product image -->
                            <button type="submit" class="btn btn-outline-primary btn-sm">Add to Basket</button> <!-- Submit button -->
                        </form>
                    </div>
                </div>
            </div>
        }
    </div>
</div>