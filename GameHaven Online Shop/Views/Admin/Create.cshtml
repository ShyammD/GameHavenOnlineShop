@model GameHaven.Models.Models.Game
@{
    // Sets the page title for the browser and layout
    ViewData["Title"] = "Add New Game";
}

<!-- Page heading -->
<h2>Add New Game</h2>

<!-- Form to create a new game record, posts to AdminController.Create -->
<form asp-action="Create" method="post">

    <!-- Title input field -->
    <div class="mb-3">
        <label asp-for="Title" class="form-label"></label>
        <input asp-for="Title" class="form-control" />
        <!-- Validation message for Title -->
        <span asp-validation-for="Title" class="text-danger"></span>
    </div>

    <!-- Price input field -->
    <div class="mb-3">
        <label asp-for="Price" class="form-label"></label>
        <input asp-for="Price" class="form-control" />
        <!-- Validation message for Price -->
        <span asp-validation-for="Price" class="text-danger"></span>
    </div>

    <!-- Category input field (e.g. Xbox Game, PlayStation Console, etc.) -->
    <div class="mb-3">
        <label asp-for="Category" class="form-label"></label>
        <input asp-for="Category" class="form-control" />
    </div>

    <!-- Platform input field (e.g. Xbox, PlayStation, PC) -->
    <div class="mb-3">
        <label asp-for="Platform" class="form-label"></label>
        <input asp-for="Platform" class="form-control" />
    </div>

    <!-- Release date input field -->
    <div class="mb-3">
        <label asp-for="ReleaseDate" class="form-label"></label>
        <input asp-for="ReleaseDate" class="form-control" />
    </div>

    <!-- Genre input field (e.g. Action, RPG, Sports) -->
    <div class="mb-3">
        <label asp-for="Genre" class="form-label"></label>
        <input asp-for="Genre" class="form-control" />
    </div>

    <!-- Slug input field (used in URLs for SEO-friendly links) -->
    <div class="mb-3">
        <label asp-for="Slug" class="form-label"></label>
        <input asp-for="Slug" class="form-control" />
    </div>

    <!-- Game description text area -->
    <div class="mb-3">
        <label asp-for="Description" class="form-label"></label>
        <textarea asp-for="Description" class="form-control"></textarea>
    </div>

    <!-- Image URL input field (path or link to game image) -->
    <div class="mb-3">
        <label asp-for="ImageUrl" class="form-label"></label>
        <input asp-for="ImageUrl" class="form-control" />
    </div>

    <!-- Submit button to save game and cancel button to return to list -->
    <button type="submit" class="btn btn-success">Add Game</button>
    <a asp-action="Index" class="btn btn-secondary">Cancel</a>
</form>